# --- File: .env.example ---

# --- File: .env ---
LOCAL = True
MEDIA_ROOT =
STAGE_DIR = MEDIA_ROOT / "stage"
RAW_DIR = MEDIA_ROOT / "raw"
JPG_DIR = MEDIA_ROOT / "jpg"

# --- Additional Config ---
WIKI_API_URL=https://en.wikipedia.org/w/api.php

# --- Default confidence threshold for search ---
DEFAULT_CONFIDENCE_THRESHOLD=0.6

# --- URL for syncing species data ---
SEED_MANIFEST_URL="https://api.wildlife.readcommitted.com/manifest"

# --- Default weights for cosine similarity ---
DEFAULT_IMAGE_WEIGHT = 0.6
DEFAULT_TEXT_WEIGHT = 0.4